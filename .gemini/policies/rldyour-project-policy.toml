name = "rldyour-project-policy"
version = "1.3.4"
scope = "gemini-adapter"

[execution]
standard_mode = "owner is the orchestration layer"
cmux_mode = "visible terminal sessions only"
headless_prompt = "smoke, doctor, and CI only"

[forbidden]
background_orchestrator = true
headless_orchestrator = true
daemon_orchestrator = true
worker_as_background_job = true
